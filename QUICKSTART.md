# 🚀 Быстрый старт

## 1. Создайте виртуальное окружение

```powershell
py -3.13 -m venv venv
```

## 2. Активируйте окружение

**PowerShell:**
```powershell
venv\Scripts\Activate.ps1
```

**CMD:**
```cmd
venv\Scripts\activate.bat
```

## 3. Установите зависимости

```powershell
pip install -r requirements.txt
```

## 4. Настройте .env файл

Скопируйте `.env.example` в `.env`:
```powershell
copy .env.example .env
```

Отредактируйте `.env` и укажите:
- `BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather)
- `WHITELIST_USER_IDS` - узнайте свой ID у [@userinfobot](https://t.me/userinfobot)
- `PATIENT_NUMBER` и `PATIENT_BIRTHDAY` - ваши данные
- `DEPARTMENT_IDS` - ID отделений для мониторинга (52, 53, 54)

## 5. Запустите бота

```powershell
python main.py
```

## 6. Используйте в Telegram

1. Найдите вашего бота
2. Отправьте `/start`
3. Используйте `/check` для проверки записей
4. Бот автоматически будет присылать уведомления о новых талонах

## Команды бота

- `/start` - Включить уведомления
- `/stop` - Выключить уведомления
- `/status` - Статус мониторинга
- `/check` - Проверить записи сейчас

## Устранение проблем

### Ошибка "BOT_TOKEN не установлен"
Проверьте что файл `.env` создан и содержит корректный токен бота.

### Бот не отвечает
1. Проверьте что ваш User ID добавлен в `WHITELIST_USER_IDS`
2. Убедитесь что бот запущен (проверьте консоль)
3. Отправьте `/start` в боте

### Не приходят уведомления
1. Проверьте что выполнили `/start`
2. Проверьте статус командой `/status`
3. Проверьте что ваш ID в белом списке
