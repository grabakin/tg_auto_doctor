# 🏥 Telegram бот для мониторинга записей к врачам

Автоматический мониторинг появления новых талонов на запись к врачам на портале zdrav.mosreg.ru

## 📋 Возможности

- ⏰ Автоматическая проверка доступных записей каждые N минут
- 🔔 Мгновенные уведомления о новых талонах
- 👥 Система белого списка пользователей
- 📊 Отслеживание изменений в расписании
- 💾 Сохранение истории в SQLite
- 🔒 Защита от дублирования уведомлений

## 🚀 Установка

### 1. Клонирование проекта

```bash
cd tg_auto_doctor
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
```

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка окружения

Скопируйте `.env.example` в `.env`:
```bash
copy .env.example .env
```

Отредактируйте `.env` файл:

```env
# Получите токен у @BotFather в Telegram
BOT_TOKEN=your_bot_token_here

# Ваши данные для API запросов
PATIENT_NUMBER=5093499728001687
PATIENT_BIRTHDAY=2005-06-21

# Department IDs для мониторинга (через запятую)
DEPARTMENT_IDS=52,53,54

# Интервал проверки в минутах
CHECK_INTERVAL=3

# Telegram User IDs в белом списке (через запятую)
# Узнайте свой ID у @userinfobot
WHITELIST_USER_IDS=123456789,987654321
```

## 📱 Как получить Telegram User ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему любое сообщение
3. Он пришлет вам ваш User ID
4. Добавьте этот ID в `WHITELIST_USER_IDS` в `.env` файле

## ▶️ Запуск

```bash
python main.py
```

## 🤖 Команды бота

- `/start` - Включить уведомления о новых записях
- `/stop` - Выключить уведомления
- `/status` - Показать текущий статус мониторинга
- `/check` - Выполнить проверку прямо сейчас

## 🏗️ Структура проекта

```
tg_auto_doctor/
├── config.py              # Конфигурация приложения
├── main.py               # Точка входа
├── requirements.txt      # Зависимости
├── .env                  # Настройки окружения (не в git)
├── .env.example         # Пример настроек
├── bot/
│   ├── handlers.py      # Обработчики команд
│   ├── middlewares.py   # Middleware для проверки доступа
│   └── notifier.py      # Сервис уведомлений
├── api/
│   ├── client.py        # HTTP клиент для API
│   └── parser.py        # Парсинг ответов
├── monitor/
│   ├── tracker.py       # Отслеживание изменений
│   └── scheduler.py     # Планировщик задач
├── database/
│   └── db.py            # Работа с SQLite
└── utils/
    └── formatter.py     # Форматирование сообщений
```

## 📊 База данных

Бот использует SQLite для хранения:

- **users** - Пользователи и их настройки
- **doctors** - Информация о врачах и кабинетах
- **schedule_state** - История состояний расписания
- **notifications** - Лог отправленных уведомлений

## ⚙️ Как это работает

1. **Мониторинг:** Бот периодически (каждые N минут) запрашивает API для указанных отделений
2. **Сравнение:** Сравнивает текущие данные с предыдущими из базы данных
3. **Обнаружение:** Находит новые талоны или освободившиеся записи
4. **Уведомление:** Отправляет форматированные сообщения всем активным пользователям
5. **Сохранение:** Обновляет состояние в базе данных

## 🔒 Безопасность

- Только пользователи из белого списка могут использовать бота
- Токен бота и персональные данные хранятся в `.env` файле
- `.env` файл добавлен в `.gitignore` и не попадает в git

## 🛠️ Технологии

- **aiogram 3.x** - Асинхронная библиотека для Telegram Bot API
- **aiohttp** - Асинхронные HTTP запросы
- **aiosqlite** - Асинхронная работа с SQLite
- **APScheduler** - Планировщик задач
- **python-dotenv** - Загрузка переменных окружения

## 📝 Логирование

Логи сохраняются в:
- Консоль (stdout)
- Файл `bot.log`

## ❓ Часто задаваемые вопросы

**Q: Как добавить нового пользователя?**  
A: Добавьте его Telegram User ID в `WHITELIST_USER_IDS` в `.env` файле и перезапустите бота.

**Q: Как изменить интервал проверки?**  
A: Измените значение `CHECK_INTERVAL` в `.env` файле (в минутах).

**Q: Как добавить новое отделение для мониторинга?**  
A: Добавьте department ID в `DEPARTMENT_IDS` в `.env` файле через запятую.

**Q: Бот не отправляет уведомления**  
A: Проверьте, что вы в белом списке и выполнили `/start` команду.

## 📄 Лицензия

Свободное использование для личных целей.

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Правильность токена бота
2. Наличие вашего ID в белом списке
3. Логи в `bot.log` файле
